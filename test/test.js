const assert = require('assert');
const io = require('./../src/util/IO');

describe('String', () => {
	describe('hello()', () => {
		it('should return Hello Xerpa! when the function is called', () => {
			assert.equal('Hello Xerpa!', 'Hello Xerpa!');
		});
	});
});